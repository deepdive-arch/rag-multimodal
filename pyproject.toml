[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 120
target-version = "py312"
exclude = [".agents", ".venv", ".tmp", "frontend/node_modules", "frontend/.next"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
