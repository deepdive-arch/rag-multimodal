# Ingestão

Use `python tools/ingest.py arquivo.pdf` ou passe um diretório recursivo. A ingestão valida assinatura, tamanho e duração, deduplica por SHA-256, persiste derivados e registra falhas no catálogo.
