# Testes

Execute `ruff check .`, `pytest -q`, `npm run lint`, `npm run typecheck` e `npm run build` dentro de `frontend/`. Para o fluxo manual, configure as chaves e confirme que o índice está pronto antes de ingerir arquivos.
