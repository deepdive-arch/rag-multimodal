# Setup local

1. Copie `.env.example` para `.env`.
2. Preencha `GOOGLE_API_KEY` e `PINECONE_API_KEY`.
3. Instale `requirements.txt` e as dependências de `frontend/`.
4. Execute `python tools/setup_pinecone.py`.
5. Inicie o backend e o frontend conforme o README.
