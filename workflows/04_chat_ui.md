# Interface de chat

A UI local mantém mensagens e preferências no `localStorage`. Uploads, filtros, cancelamento de query, fontes multimodais, feedback e exportação Markdown são executados pelo cliente da API.
